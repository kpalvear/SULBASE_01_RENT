{
  "name": "@sulbase/rent-repo",
  "private": true,
  "scripts": {
    "build": "npx pnpm@9 --dir OpenProperty install --frozen-lockfile && npx pnpm@9 --dir OpenProperty run build",
    "deploy": "npx pnpm@9 --dir OpenProperty exec wrangler deploy --env=\"\"",
    "typecheck": "npx pnpm@9 --dir OpenProperty run typecheck"
  }
}
